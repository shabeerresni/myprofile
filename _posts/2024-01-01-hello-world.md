---
title: "Your first post title here"
date: 2024-01-01
excerpt: "A one-line summary of the post."
slug: hello-world
---

Welcome to the blog. This is a **sample post** so you can verify titles, dates, excerpts, and Markdown rendering on [the blog index](/blog/) and [this post’s page](/blog/post.html?slug=hello-world).

## What you can do next

- Add new Markdown files under `_posts/` using the same frontmatter block (`title`, `date`, `excerpt`, `slug`).
- Register each post in `blog/posts-manifest.json` so it appears in the list (the index reads that file; GitHub Pages does not auto-list folders).
- Use **Blog Studio** at `/blog/studio.html` (not linked in the public nav) to draft with AI and optionally publish via the GitHub API.
